<nav class="navbar navbar-default">
  <div class="container-fluid">
    <div class="navbar-header">
      <a class="navbar-brand" href="/">The New IMDB</a>
    </div>
    <% if (user) { %>
    <span class="navbar-text"> User: <%= user.displayName %> </span>
    <% } else {%>
      <% } %>

    <ul class="nav navbar-nav">
        <% if (!user) { %>
      <li><a href="/login_github">Login with GitHub</a></li>
      <% } else {%>
        <li><a href="/logout">Logout</a></li>
        <% } %>
      <li><a href="/favorites">Favorites</a></li>
      <li>
        <select name="login_options" class="form-control" onchange="window.location=this.value">
          <option value="/">Please choose your login options here!</option>
          <option value="/login_github">Login with GitHub</option>
          <option value="/users/login">Login with Custom Account</option>
          </select>
        </li>
    </ul>

    <ul class="nav navbar-nav navbar-right">
      <li>
          <form action="/search?source=hp" method="post" id="search-form" class="form-inline">
            <div class="form-group">
              <select name="cat" class="form-control" id="cat">
                <option value="movie">Movie Title</option>
                <option value="person">Actor</option>
              </select>
            </div>
            
            <input type="text" id="movie" placeholder="Search" name="movieSearch" />
            <input type="submit" class="btn btn-primary" />
            
          </form>
      </li>      
    </ul>
  </div>